# Windows build resources

`electron-builder` uses this directory for Windows installer resources. The
foundation build intentionally falls back to Electron's valid default Windows
icon until a final, approved Lyor `.ico` asset is available; no temporary remote
or invalid placeholder icon is packaged.
